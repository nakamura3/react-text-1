import React from 'react';

const css = {
  color: 'red',
  fontSize: 24,
  width: '100%',
  border: '1px solid #000'
};

const App = () => (
  <div style={css} >CSS in JS</div>
);

export default App;